<?php
    $var = 10;
    $var++;
      echo $var;                    // The increment operators are used to increment a variable's value by one.
                                    // The post-increment returns the original value before it changes the variable
?>
